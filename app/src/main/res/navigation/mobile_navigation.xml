<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="edu.edugain.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
        android:id="@+id/nav_add_course"
        android:name="edu.edugain.admin_tool_panel.AddCourseFragment"
        android:label="Add Course"
        tools:layout="@layout/fragment_add_course" />

    <fragment
        android:id="@+id/nav_add_dept"
        android:name="edu.edugain.admin_tool_panel.AddDepartmentFragment"
        android:label="Add Department"
        tools:layout="@layout/fragment_add_dept" />
</navigation>